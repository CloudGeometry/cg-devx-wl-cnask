import type { Meta } from '@storybook/react';
import { FormLayout } from './form-components';

const Story: Meta<typeof FormLayout> = {
  component: FormLayout,
  title: 'FormLayout',
};
export default Story;

export const Primary = {
  args: {},
};
